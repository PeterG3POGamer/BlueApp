<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo normal -->
    <solid android:color="#F2F2F2" /> <!-- Color de fondo -->

    <!-- Esquinas redondeadas -->
    <corners android:radius="0dp" /> <!-- Ajusta el radio según tus preferencias -->

    <!-- Borde con sombra -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" /> <!-- Color del borde -->

    <!-- Efecto de sombra -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="1dp" />
</shape>
